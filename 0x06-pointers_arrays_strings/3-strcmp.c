#include "main.h"
#include <string.h>

/**
 * _strcmp - functions that compares two strings
 * @s1: first param
 * @s2: second param
 * Return: an integer
 */
int _strcmp(char *s1, char *s2)
{
	int result;

	strcmp(s1, s2);
	return (result);
}
